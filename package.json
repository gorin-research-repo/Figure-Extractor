{
  "name": "figure-extractor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Convert journal PDF pages to high-resolution SVGs, then crop and download figures — AugmentedMD",
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "npm run build && python3 -m http.server 8080",
    "test": "npm run build --silent && node --test"
  },
  "dependencies": {
    "pdfjs-dist": "^6.2.108"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
